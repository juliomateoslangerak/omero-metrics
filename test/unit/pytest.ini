[pytest]
# Override the Django settings for unit tests — these tests don't need Django/OMERO
# The pyproject.toml sets DJANGO_SETTINGS_MODULE which requires OMERODIR
# Setting an empty value effectively unsets it for pytest-django
addopts = -p no:django
